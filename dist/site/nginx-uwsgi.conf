server {
    listen 80;
    server_name __SERVERNAME__;
    root __DOCROOT__;

    location / {
		include uwsgi_params;
		uwsgi_pass __SERVERNAME__:9999;
		uwsgi_param UWSGI_CHDIR __DOCROOT__;
		uwsgi_param UWSGI_SCRIPT main.wsgi;
    }
}
