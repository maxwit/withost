#!/usr/bin/python

import os

def setup(conf, apps):
	pass
	#file_svr = 'www.maxwit.org'
	#font_dir = '/usr/share/fonts/truetype/extra'

	#os.system('sudo mkdir -vp ' + font_dir)
	#for font in ['simsun.ttc', 'simhei.ttf']:
	#	os.system('wget -P %s http://%s/pub/fonts/%s' % (font_dir, file_svr, font))
	#os.system('fc-cache -f')
